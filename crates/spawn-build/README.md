# spawn-build

`spawn-build` is the asset build pipeline library at the base of the engine's tooling chain.

See [DESIGN.md](DESIGN.md) for architecture, design decisions, constraints, and Phase 1 scope.
