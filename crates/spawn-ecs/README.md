# spawn-ecs

`spawn-ecs` is the entity-component-system core of the engine: it owns entity identity, component storage, queries over that storage, and the scheduler that drives systems each frame.

See [DESIGN.md](DESIGN.md) for architecture, design decisions, constraints, and Phase 1 scope.
